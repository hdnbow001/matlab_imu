注意！！
此处的代码需要配合带有UART串口的IMU传感器实物使用
IMU串口配置为115200,8位，无流控
命令格式参考stm32开发文档

使用时，需要用sscom32等串口数据工具先下发IMU传输指令03——打开传感器原始数据传输/05——打开传感器pitch、roll角度传输
然后关闭sscom上的串口（此时IMU仍在持续发送，但是sscom释放串口）
然后运行本目录中的m脚本就可以获得串口数据

任何没有完成采样中途退出的行为，将导致matlab占用串口不释放，此时需要手工终止matlab运行，并执行fclose(s)来释放串口